// Same generated image as opengraph-image.tsx — Next's file-convention
// resolution doesn't reliably fall back from Twitter card metadata to
// the Open Graph image on its own, so this re-exports the same
// generator rather than risk Twitter/X showing no preview at all.
export { default, alt, size, contentType } from './opengraph-image'
